version: "3.9"

services:
  backend:
    image: ghcr.io/mannycarreiro/jinja2-live-parser/backend
    container_name: j2live-backend
    restart: unless-stopped
    ports:
      - "6000:5000"
    environment:
      - FLASK_ENV=production
      - PYTHONDONTWRITEBYTECODE=1
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s
    networks:
      - j2live-net

  frontend:
    image: ghcr.io/mannycarreiro/jinja2-live-parser/frontend
    container_name: j2live-frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - j2live-net

networks:
  j2live-net:

